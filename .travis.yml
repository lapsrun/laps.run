before_install:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > before_install-start.iso8601
  - mkdir -p .timing/
  - mv before_install-start.iso8601 .timing/
  - wget https://github.com/gohugoio/hugo/releases/download/v0.48/hugo_0.48_Linux-64bit.tar.gz
  - tar xzvf hugo_0.48_Linux-64bit.tar.gz
  - sudo mv hugo /usr/local/bin/
  - sudo wget https://github.com/stedolan/jq/releases/download/jq-1.5/jq-linux64 -O /usr/local/bin/jq
  - sudo chmod o+x /usr/local/bin/jq
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/before_install-end.iso8601

install:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/install-start.iso8601
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/install-end.iso8601

before_script:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/before_script-start.iso8601
  - hugo version
  - jq --version
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/before_script-end.iso8601

script:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/script-start.iso8601
  - ./bin/travis-script.sh
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/script-end.iso8601

# this has to be after_success because deploy doesn't run for pr builds
after_success:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_success-start.iso8601
  - ./bin/preview-url-to-pr-comment.sh
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_success-end.iso8601

after_failure:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_failure-start.iso8601
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_failure-end.iso8601

before_deploy:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/before_deploy-start.iso8601
  - make public/deploy/latest.json
  - cp public/deploy/latest.json public/deploy/$(TRAVIS_BUILD_NUMBER).json
  - sed -i -e "s/123456abcdef/$TRAVIS_COMMIT/" public/index.html
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/before_deploy-end.iso8601

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY_ID
    secret_access_key: $AWS_SECRET_ACCESS_KEY
    local_dir: public/
    bucket: "laps.run"
    skip_cleanup: true
    on:
      repo: tphummel/laps.run
      branch: master
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY_ID
    secret_access_key: $AWS_SECRET_ACCESS_KEY
    local_dir: public/
    bucket: laps.run-preview
    upload-dir: $TRAVIS_BRANCH
    skip_cleanup: true
    on:
      repo: tphummel/laps.run
      all_branches: true
      condition: "$TRAVIS_BRANCH != master"

after_deploy:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_deploy-start.iso8601
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_deploy-end.iso8601

after_script:
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_script-start.iso8601
  - date -u +"%Y-%m-%dT%H:%M:%SZ" > .timing/after_script-end.iso8601
  - tail -n +1 -- .timing/*
